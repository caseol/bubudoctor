$("#modal-window").find(".modal-content").html("<%= j (render 'edit') %>");
$("#modal-window").modal(_init()).animate({ scrollTop: 0 }, 'slow');

if ($("#patients_birth").val() == "" || $("#patients_birth").val() === undefined){
    bind_patient_birth()
}
else{
    setPatientAge($("#patients_birth").val());
}

